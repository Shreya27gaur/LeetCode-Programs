/* Write your PL/SQL query statement below */
SELECT D.NAME AS Department,
T.NAME AS Employee,
T.SALARY AS Salary
FROM
(SELECT ID,NAME,SALARY, DEPARTMENTID,MAX(SALARY) OVER(PARTITION BY DEPARTMENTID) ma
FROM EMPLOYEE) T, DEPARTMENT D
WHERE T.SALARY = T.MA
AND T.DEPARTMENTID = D.ID;